import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReviews = () => {
    useTitle('MyReviews')
    return (
        <div>
            <h1>this is my reviews section</h1>
        </div>
    );
};

export default MyReviews;